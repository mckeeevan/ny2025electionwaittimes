name: Fetch NY Voter Wait Times (NYC)

on:
  schedule:
    # - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write   # 👈 THIS is what allows git push

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Fetch and save data
        run: node fetch-ny-voter-wait-times.js

      - name: Commit and push data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git commit -m "NYC voter wait times snapshot $(date -u +'%Y-%m-%d %H:%M:%S UTC')" || echo "No changes to commit"
          git push